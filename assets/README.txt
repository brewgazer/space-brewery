Run once to download free CC0 / demo assets (patron model + floor textures):

  npm run fetch-assets

Then serve the folder over HTTP (e.g. npx serve) so GLB/texture paths load.

See ATTRIBUTION.md for credits.
